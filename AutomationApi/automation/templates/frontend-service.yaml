apiVersion: v1
kind: Service
metadata:
  labels:
    name: {{.Values.releaseName}}
  name: {{.Values.releaseName}}
spec:
  ports:
    - protocol: "TCP"
      port: {{.Values.servicePort}}
      targetPort: 4200
  selector:
    app: {{.Values.releaseName}}
